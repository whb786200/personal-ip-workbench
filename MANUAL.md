# 使用说明书

## 1. 打开系统

双击 `START-HERE.bat`。

系统会打开：

- 主工作台：http://127.0.0.1:8787
- 中文后台：http://127.0.0.1:8787/admin-cn.html
- IP 形象生成器：http://127.0.0.1:8787/ip-generator.html
- 视频生成：http://127.0.0.1:8501

## 2. 中文后台怎么用

进入中文后台后，左侧有四个模块：

- 内容任务：记录选题、脚本、素材、成片、发布状态
- IP 画像：维护账号定位、人群、承诺、风格
- 素材库：保存链接、笔记、金句和参考素材
- 发布排期：规划公众号、小红书、抖音、视频号等内容

点击一行可以编辑；右上角“新增”可以添加记录；“同步到工作台”会把后台数据同步到主工作台。

## 3. 生成 IP 形象提示词

打开 IP 形象生成器：

```text
http://127.0.0.1:8787/ip-generator.html
```

填写 IP 类型、形象 brief、画风、强化特征和避免元素，点击“生成提示词”。复制生成结果后，可粘贴到支持图片生成的工具中制作头像、数字分身或表情包。

## 4. 生成视频

打开 MoneyPrinterTurbo：

```text
http://127.0.0.1:8501
```

先在 `config.toml` 填好 DeepSeek 和 Pexels Key，然后在页面里输入主题或脚本生成视频。

## 5. 每日运营流程

1. 在“素材库”记录灵感和链接。
2. 在“内容任务”添加选题。
3. 用 IP 形象生成器生成头像、数字分身或表情包提示词。
4. 用主工作台生成脚本提示词。
5. 到 MoneyPrinterTurbo 生成短视频。
6. 在“发布排期”安排平台和日期。
7. 发布后回填链接和状态。

## 6. 故障处理

运行自检：

```powershell
.\verify-workbench.ps1
```

如果中文后台打不开：

```powershell
.\run-workbench.ps1
```

如果 PocketBase 需要重新初始化：

```powershell
.\init-pocketbase.ps1
```
